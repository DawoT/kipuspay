{
  "name": "@kipuspay/domain-catalog",
  "version": "0.0.0",
  "private": true,
  "description": "KipusPay — dominio de catálogo: clasificación de escaneo por namespace (edge 1A) y alta rápida (regla 34)",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint src --max-warnings 0",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run --coverage",
    "test:unit": "vitest run --coverage",
    "build": "true"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.10",
    "eslint": "^10.8.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
