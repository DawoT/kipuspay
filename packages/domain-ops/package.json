{
  "name": "@kipuspay/domain-ops",
  "version": "0.0.0",
  "private": true,
  "description": "KipusPay — dominio de operaciones: handoff de turno (regla 35) y equipo/invitaciones (regla 36)",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint src --max-warnings 0",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run --coverage",
    "test:unit": "vitest run --coverage",
    "build": "true"
  },
  "dependencies": {
    "@kipuspay/domain-catalog": "workspace:*"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.10",
    "eslint": "^10.8.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
