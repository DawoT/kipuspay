# Gitleaks — escaneo de secretos en commits (Quality Gate CAL-05 / Sprint 2).
# Nivel de umbral: solo secreto real; ledger e hashes de control D1 se permiten.

title = "KipusPay gitleaks config"

[extend]
useDefault = true

[allowlist]
paths = [
  '''docs/LEDGER\.md''',
  '''docs/architecture/.*\.md''',
]
regexes = [
  '''CRC32\([0-9a-f]+\)''',
]
