
apps/*/build/
apps/*/functions/
apps/*/.svelte-kit/
apps/*/.wrangler/
# Artefactos de E2E / screenshots locales
# Artefactos staff (PEM, envelope, XML firmado). No mezclar con doctrina.
build/
.claude/
# Config y skills de agentes locales (tooling; no doctrina)
coverage/
/current-*.png
.cursor/
.dev.vars
dist/
.DS_Store
# Editor / SO
.env
!.env.example
# Estado local wrangler / Miniflare (nunca versionar)
# Frameworks y compilación de apps
/home-*.png
.idea/
# Ignorar artefactos locales; los docs maestros y el código fuente siempre se versionan.
# KipusPay — repo de especificación + monorepo de implementación (Arquitectura §1.1)
*.log
# Node / build tooling
node_modules/
*.p12
packages/*/.wrangler/
*.pem
*.pfx
.playwright-cli/
.playwright-mcp/
**/playwright-report/
.pnpm-store/
*.pyc
__pycache__/
# Python de los checks (scripts/checks/*.py se importan entre sí y generan caché)
/quipu-premium-*.png
# Scripts TEMP de auditoría de diseño (wave 4-5; no versionar)
scripts/tmp/
# Secretos / entorno
*.swp
**/test-results/
Thumbs.db
tmp-staff/
*.tsbuildinfo
.turbo/
.vscode/
.wrangler/
# Wrangler dev secrets (nunca versionar)
